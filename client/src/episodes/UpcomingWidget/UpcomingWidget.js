import React from 'react';

import { Section } from '../../shared/sections';
import { AvailableList } from '../AvailableList';

export default () => {
  return (
    <Section>
      <AvailableList />
    </Section>
  );
};
