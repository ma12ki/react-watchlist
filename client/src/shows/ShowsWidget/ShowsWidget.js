import React from 'react';

import { Section } from '../../shared/sections';
import { ShowsList } from '../ShowsList';

export default () => {
  return (
    <Section>
      <ShowsList />
    </Section>
  );
};
