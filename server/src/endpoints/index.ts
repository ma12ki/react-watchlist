import { showsModule } from './shows';
import { seasonsModule } from './seasons';
import { episodesModule } from './episodes';

const modules = [
  showsModule,
  seasonsModule,
  episodesModule,
];

export { modules };
