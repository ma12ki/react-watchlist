import { authModule } from './auth';
import { usersModule } from './users';
import { showsModule } from './shows';

const modules = [
  authModule,
  usersModule,
  showsModule,
];

export { modules };
